/*Q15. Write a java program to print all ASCII characters with their values.*/

import java.util.Scanner;
class ASCII1 
{
	public static void main(String[]args)
	{
		Scanner sc = new Scanner(System.in);
		System.out.println("Enter character from keyboard");
         char ch;
        char  = sc.nextLine().charAt(0)
        System.out.printf("Before conversion ,ch");
      if
	  {
	  (ch>=65 && ch <= 90);
      (ch = (char) ( (int) ch + 32);
	  }
      else
	  {
           ch = (char) ( (int)ch-32);
         
    	 System.out.printf("After conversion %c\n",ch);
       }
	}
	
}
